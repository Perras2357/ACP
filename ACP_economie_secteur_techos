clear all

%1.lire les données dans le fichier à l'aide de la fonction matlab

Matrice_data = load("techno_info.txt");

%2. calcul des valeurs propres et vecteurs propres

%transformation de la matrice Matrice_data : pour y arriver, on standardise la matrice afin de la centrer et la réduire
Xc= Matrice_data - mean(Matrice_data);

%standardisation
Xs = Xc ./ std(Xc);

%calcul de M (matrice de covariance) =Xt*X/n-1
n = size(Xs,1);
M = (Xs' * Xs)/(n-1);

% Diagonalisation de V : E= matrice des vecteurs propres et D matirce des valeurs propres

[E, D] = eig(M);


